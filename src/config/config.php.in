<?php
/*
 * @author Michael Mifsud <info@tropotek.com>
 * @link http://www.tropotek.com/
 * @license Copyright 2015 Michael Mifsud
 */
$config = \Tk\Config::getInstance();

// Database access
$config['db.type'] = 'mysql';
$config['db.host'] = 'localhost';
$config['db.name'] = 'table';
$config['db.user'] = 'username';
$config['db.pass'] = 'password';

/*
 * DB secret API key used for the mirror command
 * Use this  key for the mirror command in a dev environment.
 * Keep this key secret. Access to the sites DB can be gained with it.
 */
//$config['db.skey'] = '{Add secret key here}';

// Microsoft SSO settings
// Azure: https://portal.azure.com/
$config['auth.microsoft.enabled'] = false;
// tenant ID or `common` for multi-tenant
$config['auth.microsoft.tenantid'] = 'common';
$config['auth.microsoft.clientid'] = '';
$config['auth.microsoft.logout'] = 'https://login.microsoftonline.com/common/wsfederation?wa=wsignout1.0';
$config['auth.microsoft.scope'] = 'openid%20offline_access%20profile%20user.read';
// method = 'certificate' or 'secret'
$config['auth.microsoft.oauth.method'] = 'secret';
$config['auth.microsoft.oauth.secret'] = '';
// on Windows, the certificate paths should be in the form c:/path/to/cert.crt
//$config['auth.microsoft.oauth.certfile'] = '/path/to/certificate.crt';
//$config['auth.microsoft.oauth.keyfile'] = '/path/to/privatekey.pem';

/*
 * Enable exception emails
 */
//$config['system.email.exception'] = array('user@example.com');

/*
 * Send copies of all system emails to these recipients (not error emails)
 */
//$config['mail.bcc'] = array('user1@example.edu.au');

/*
 * Enable logging of triggered events
 * Default: false
 */
//$config['event.dispatcher.log'] = true;

/*
 * Debug settings
 */
//$config['debug'] = true;
//$config['log.level'] = \Psr\Log\LogLevel::DEBUG;
//$config['log.path'] = '/home/user/log/error.log';
//$config['system.debug.email'] = 'dev@example.com';

// Used for the Mirror command.
//if ($config->isDebug()) {
    // Used for the Mirror command
    //$config['mirror.db']   = 'https://localhost/util/mirrorDb';
    //$config['mirror.data'] = 'https://localhost/util/mirrorData';
//}
